---
labels: ['env']
description: 'My custom env for Vue.'
---

My custom env for Vue.

## Usage

```bash
# create a Vue workspace
bit new vue my-vue-workspace --aspect <your-env-id>

# add a Vue component
bit create vue <component-name>

# add a Vue composable
bit create vue-composable <component-name>

# add a Vue app
bit create vue-app <component-name>
```
